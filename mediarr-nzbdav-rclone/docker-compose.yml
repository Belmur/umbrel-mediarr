services:
  app_proxy:
    environment:
      # The format here is: <app-id>_<docker-service-name>_1
      APP_HOST: mediarr-nzbdav-rclone_nzbdav-rclone_1

  nzbdav-rclone:
    image: rclone/rclone:latest
    restart: unless-stopped
    volumes:
      - ${APP_DATA_DIR}/config/rclone.conf:/config/rclone/rclone.conf:ro
      - ${APP_DATA_DIR}/config/cache:/config/rclone/cache
      - ${UMBREL_ROOT}/home/Downloads/nzbdav:/Downloads/nzbdav:rshared
    environment:
      # Change these IDs to match your Docker user that you got from above
      - PUID=1000
      - PGID=1000
      - TZ=Etc/UTC
    cap_add:
      - SYS_ADMIN
    security_opt:
      - apparmor:unconfined
    devices:
      - /dev/fuse:/dev/fuse:rwm
    depends_on:
      - mediarr-nzbdav_nzbdav_1
    command: >-
      mount nzbdav: /Downloads/nzbdav
      --uid=1000
      --gid=1000
      --allow-other
      --links
      --use-cookies
      --vfs-cache-mode=full
      --vfs-cache-max-size=20G
      --vfs-cache-max-age=24h
      --buffer-size=1024
      --vfs-read-ahead=512M
      --dir-cache-time=20s
      --allow-non-empty
      --cache-dir=/config/rclone/cache
